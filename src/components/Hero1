.hero-section {
    display: flex;
    align-items: center;
    gap: 40px;
    padding: 60px 0;
    position: relative;
    min-height: 80vh;
    background: transparent; /* Важно! */
}

.content {
    flex: 0 0 50%;
    animation: fadeIn 1s ease-out;
    z-index: 2;
}

/* Анимация вылета справа */
@keyframes slideFromRight {
    0% {
        transform: translateX(100%) scale(1.2);
        opacity: 0;
    }
    100% {
        transform: translateX(15%) scale(0.85);
        opacity: 1;
    }
}

h1 {
    font-family: 'Druk', sans-serif;
    font-size: 2.5rem;
    text-transform: uppercase;
    margin-bottom: 20px;
    line-height: 1.2;
}


.services-list {
    margin: 10px 0;
}

.description {
    font-weight: bold;
    font-size: 1.3rem;
    margin-bottom: 10px;
}

.services-list ul {
    list-style-type: none;
    padding: 0;
    margin: 0;
    columns: 2;
    column-gap: 40px;
}

.services-list li {
    margin-bottom: 8px;
    position: relative;
    padding-left: 20px;
    break-inside: avoid;
}

.services-list li::before {
    content: "•";
    position: absolute;
    left: 0;
    color: #25d366;
    font-size: 1.2em;
}
@media (max-width: 768px) {
    .services-list ul {
        columns: 1;
    }
}

.whatsapp-button {
    padding: 15px 30px;
    background-color: #47be33;
    color: white;
    border: none;
    border-radius: 4px;
    font-size: 1rem;
    cursor: pointer;
    transition: background 0.3s;
}

.whatsapp-button:hover {
    background-color: #449c36;
}

.moon-image {
    animation: slideFromRight 1.5s ease-out forwards;
    flex: 1;
    position: relative;
    z-index: 1;

}

.moon-image img {
    width: 130%;
    max-width: 600px;
    height: auto;
    display: block;
}

@keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}

/* Мобильная версия */
@media (max-width: 768px) {
    .hero-section {
        flex-direction: column;
        padding: 20px;
    }

    .content {
        margin-left: 0;
        padding-right: 0;
        max-width: 100%;
    }

    .moon-image {
        order: -1;
        margin-left: 0;
        width: 100%;
        animation: slideFromRightMobile 1s ease-out forwards;
    }

    @keyframes slideFromRightMobile {
        0% {
            transform: translateX(100%) scale(1.1);
            opacity: 0;
        }
        100% {
            transform: translateX(10%) scale(0.9);
            opacity: 1;
        }
    }
}





JS
import React, { useEffect, useRef } from 'react';
import './HeroSection.css';

const HeroSection = () => {
    const truckRef = useRef(null);

    useEffect(() => {
        // Сохраняем текущее значение ref в переменную
        const currentTruckRef = truckRef.current;
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('animate-truck');
                }
            });
        }, { threshold: 0.1 });

        if (currentTruckRef) {
            observer.observe(currentTruckRef);
        }

        return () => {
            if (currentTruckRef) {
                observer.unobserve(currentTruckRef);
            }
        };
    }, []); // Пустой массив зависимостей, эффект выполняется только при монтировании

    return (
            <section className="hero-section">
                <div className="content">
                    <h1>СПУТНИК - ВОЗМОЖНОСТИ СРЕДИ НАС</h1>
                    <p className="description">Специализируется на организации мультимодальных перевозок</p>

                    <div className="services-list">
                        <p>ПЕРЕВОЗКИ РАЗЛИЧНЫХ КАТЕГОРИЙ ГРУЗОВ:</p>

                        <ul>
                            <li>Контейнеров</li>
                            <li>Скоропортящихся продуктов</li>
                            <li>Негабаритных и тяжеловесных грузов</li>
                            <li>Сборных грузов</li>
                            <li>Опасных грузов</li>
                        </ul>
                    </div>

                    <button className="whatsapp-button">
                        Написать в WhatsApp
                    </button>
                </div>
                <div className="moon-image" ref={truckRef}>
                    <img src="/images/moon.png" alt="Луна" />
                </div>
            </section>
    );
};

export default HeroSection;